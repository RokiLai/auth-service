server:
  port: 8081

spring:
  cloud:
    consul:
      discovery:
        register: false
logging:
  level:
    com.example.authcenter: info
    org.mybatis: debug
    org.mybatis.spring.SqlSessionFactoryBean: debug
    org.apache.ibatis: debug
    org.springframework.cloud.consul: info
