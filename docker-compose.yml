services:
  auth-center-bootstrap:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: auth-center-bootstrap-container
    restart: always
    ports:
      - "8080:8080"
      - "9090:9090"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SERVER_PORT: 8080
      GRPC_SERVER_PORT: ${GRPC_SERVER_PORT:-9090}
      HOST_IP: ${HOST_IP}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_PORT: ${MYSQL_PORT:-3306}
      MYSQL_DATABASE: auth
      MYSQL_USERNAME: ${MYSQL_USERNAME}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_SERVICE_NAME: ${MYSQL_SERVICE_NAME:-mysql-proxy-service}
      MYSQL_JDBC_PARAMS: ${MYSQL_JDBC_PARAMS:-useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false}
      REDIS_HOST: ${REDIS_HOST}
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      REDIS_SERVICE_NAME: ${REDIS_SERVICE_NAME:-redis-proxy-service}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRE: ${JWT_EXPIRE:-3600000}
      CONSUL_HOST: ${CONSUL_HOST}
      CONSUL_PORT: ${CONSUL_PORT:-8500}
    healthcheck:
      test: ["CMD-SHELL", "bash -c '</dev/tcp/127.0.0.1/8080'"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - auth-net

networks:
  auth-net:
